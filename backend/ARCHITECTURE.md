# LinkedIn Clone Backend - Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT SIDE                              │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   React.js   │  │  Axios API   │  │ LocalStorage │          │
│  │  Components  │──│    Client    │──│  JWT Tokens  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                           │                                       │
└───────────────────────────┼───────────────────────────────────────┘
                            │
                    HTTPS / REST API
                            │
┌───────────────────────────┼───────────────────────────────────────┐
│                           ▼                                        │
│                   ┌──────────────┐                                │
│                   │  NGINX/CORS  │                                │
│                   └──────────────┘                                │
│                           │                                        │
│                   ┌──────────────┐                                │
│                   │  Rate Limiter│                                │
│                   └──────────────┘                                │
│                           │                                        │
│                   ┌──────────────┐                                │
│                   │    Helmet    │ Security Headers               │
│                   └──────────────┘                                │
│                           │                                        │
│              ┌────────────┴────────────┐                          │
│              ▼                         ▼                          │
│    ┌──────────────────┐     ┌──────────────────┐                │
│    │   Auth Routes    │     │  Public Routes   │                │
│    │  /api/auth/*     │     │   /health, /     │                │
│    └──────────────────┘     └──────────────────┘                │
│              │                                                     │
│              │                                                     │
│    ┌─────────┴─────────┐                                         │
│    │  JWT Middleware   │ Verify Token                            │
│    └─────────┬─────────┘                                         │
│              │                                                     │
│    ┌─────────┴──────────────────────────────┐                   │
│    │                                          │                   │
│    ▼                    ▼                    ▼                   │
│ ┌──────────┐      ┌──────────┐       ┌──────────┐              │
│ │  Posts   │      │  Users   │       │ Uploads  │              │
│ │ Routes   │      │ Routes   │       │ Routes   │              │
│ └────┬─────┘      └────┬─────┘       └────┬─────┘              │
│      │                  │                   │                     │
│      ▼                  ▼                   ▼                     │
│ ┌──────────┐      ┌──────────┐       ┌──────────┐              │
│ │  Post    │      │  User    │       │  Upload  │              │
│ │Controller│      │Controller│       │Controller│              │
│ └────┬─────┘      └────┬─────┘       └────┬─────┘              │
│      │                  │                   │                     │
│      ▼                  ▼                   ▼                     │
│ ┌──────────┐      ┌──────────┐       ┌──────────┐              │
│ │   Post   │      │   User   │       │Cloudinary│              │
│ │  Model   │      │  Model   │       │   API    │              │
│ └────┬─────┘      └────┬─────┘       └────┬─────┘              │
│      │                  │                   │                     │
│      └──────────────────┴───────────────────┘                   │
│                         │                                         │
│                         ▼                                         │
│                ┌─────────────────┐                               │
│                │   MongoDB Atlas  │                               │
│                │   (Database)     │                               │
│                └─────────────────┘                               │
│                                                                    │
│                    SERVER SIDE                                    │
└────────────────────────────────────────────────────────────────────┘
```

## 📦 Data Flow

### 1. User Registration/Login Flow

```
Client                   Backend                    Database
  │                         │                          │
  │──Register Request──────▶│                          │
  │  (email, password)      │                          │
  │                         │──Hash Password───────────│
  │                         │                          │
  │                         │──Create User────────────▶│
  │                         │                          │
  │                         │◀─User Created────────────│
  │                         │                          │
  │◀─JWT Token + User──────│                          │
  │                         │                          │
  │──Store Token────────────│                          │
  │  (LocalStorage)         │                          │
```

### 2. Create Post Flow

```
Client                   Backend                  Database          Cloudinary
  │                         │                          │                │
  │──Create Post───────────▶│                          │                │
  │  (text, image)          │                          │                │
  │  + JWT Token            │                          │                │
  │                         │                          │                │
  │                         │──Verify Token────────────│                │
  │                         │                          │                │
  │                         │──Upload Image────────────┼───────────────▶│
  │                         │                          │                │
  │                         │◀─Image URL───────────────┼────────────────│
  │                         │                          │                │
  │                         │──Save Post──────────────▶│                │
  │                         │                          │                │
  │                         │◀─Post Created────────────│                │
  │                         │                          │                │
  │◀─Post Data─────────────│                          │                │
```

### 3. Get Posts Feed Flow

```
Client                   Backend                    Database
  │                         │                          │
  │──Get Posts─────────────▶│                          │
  │  (page, limit)          │                          │
  │                         │                          │
  │                         │──Query Posts────────────▶│
  │                         │  (sorted, paginated)     │
  │                         │                          │
  │                         │──Populate User Data─────▶│
  │                         │                          │
  │                         │◀─Posts Array─────────────│
  │                         │  (with user info)        │
  │                         │                          │
  │◀─Paginated Posts───────│                          │
  │  (count, total, pages)  │                          │
```

## 🔐 Authentication Flow

```
┌──────────────────────────────────────────────────────────┐
│                    Authentication                         │
└──────────────────────────────────────────────────────────┘

1. User Registers/Logs In
   ↓
2. Server Validates Credentials
   ↓
3. bcrypt Verifies Password (if login)
   ↓
4. Server Generates JWT Token
   ├─ Access Token (7 days)
   └─ Refresh Token (30 days)
   ↓
5. Client Stores Tokens
   ├─ LocalStorage
   └─ Axios Interceptor
   ↓
6. Subsequent Requests Include Token
   ↓
7. Server Verifies Token
   ├─ Extract from Authorization header
   ├─ Verify signature
   ├─ Check expiration
   └─ Attach user to req.user
   ↓
8. Protected Route Accessed
```

## 🗄️ Database Schema Relationships

```
┌─────────────────┐
│      User       │
│─────────────────│
│ _id (PK)        │
│ email           │
│ password (hash) │
│ fullName        │
│ profilePicURL   │
│ profile {}      │
│ active          │
│ createdAt       │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│      Post       │
│─────────────────│
│ _id (PK)        │
│ user (FK) ──────┼─────┐
│ text            │     │
│ mediaType       │     │
│ mediaURL        │     │
│ likes []        │◀────┘ (Array of User IDs)
│ comments []     │
│ viewCount       │
│ active          │
│ createdAt       │
└─────────────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│    Comment      │
│─────────────────│
│ _id             │
│ user (FK)       │
│ text            │
│ replies []      │
│ createdAt       │
└─────────────────┘
```

## 🛡️ Security Layers

```
┌────────────────────────────────────────────────────────┐
│                    Security Layers                      │
└────────────────────────────────────────────────────────┘

Layer 1: Rate Limiting
        ↓ (100 req/15min per IP)

Layer 2: CORS
        ↓ (Allowed origins only)

Layer 3: Helmet
        ↓ (Security headers)

Layer 4: Input Validation
        ↓ (express-validator)

Layer 5: JWT Verification
        ↓ (Token signature check)

Layer 6: Password Hashing
        ↓ (bcrypt salt rounds)

Layer 7: MongoDB Injection Protection
        ↓ (Mongoose sanitization)

Layer 8: Authorization Check
        ↓ (Owner verification)

Protected Resource ✓
```

## 📊 Request Processing Pipeline

```
Incoming Request
      │
      ▼
┌──────────┐
│  CORS    │ Check origin
└────┬─────┘
      │
      ▼
┌──────────┐
│Rate Limit│ Check request count
└────┬─────┘
      │
      ▼
┌──────────┐
│ Helmet   │ Add security headers
└────┬─────┘
      │
      ▼
┌──────────┐
│Body Parse│ Parse JSON/FormData
└────┬─────┘
      │
      ▼
┌──────────┐
│ Routing  │ Match endpoint
└────┬─────┘
      │
      ▼
┌──────────┐
│Validation│ Validate input
└────┬─────┘
      │
      ▼
┌──────────┐
│Auth Check│ Verify JWT (if required)
└────┬─────┘
      │
      ▼
┌──────────┐
│Controller│ Business logic
└────┬─────┘
      │
      ▼
┌──────────┐
│  Model   │ Database operation
└────┬─────┘
      │
      ▼
┌──────────┐
│ Response │ Send JSON
└────┬─────┘
      │
      ▼
┌──────────┐
│Error Hand│ Catch errors (if any)
└──────────┘
```

## 🔄 Deployment Architecture

```
┌──────────────────────────────────────────────────────┐
│                   Production Setup                    │
└──────────────────────────────────────────────────────┘

                    ┌──────────┐
                    │  Client  │
                    │ (Vercel) │
                    └────┬─────┘
                         │
                    HTTPS/CORS
                         │
                    ┌────▼─────┐
                    │  Backend │
                    │ (Render) │
                    └────┬─────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
     ┌────▼────┐                  ┌────▼────┐
     │ MongoDB │                  │Cloudinary│
     │  Atlas  │                  │ (Images)│
     └─────────┘                  └─────────┘
```

## 📁 Folder Structure Logic

```
backend/
│
├── src/
│   │
│   ├── config/           # Configuration files
│   │   ├── db.js        # Database connection
│   │   └── cloudinary.js # Image storage config
│   │
│   ├── models/          # Data schemas (Mongoose)
│   │   ├── User.js      # User data structure
│   │   ├── Post.js      # Post data structure
│   │   └── Reaction.js  # Reaction data structure
│   │
│   ├── controllers/     # Business logic
│   │   ├── authController.js  # Auth operations
│   │   ├── postController.js  # Post operations
│   │   ├── userController.js  # User operations
│   │   └── uploadController.js # Upload operations
│   │
│   ├── routes/          # API endpoints
│   │   ├── authRoutes.js   # /api/auth/*
│   │   ├── postRoutes.js   # /api/posts/*
│   │   ├── userRoutes.js   # /api/users/*
│   │   └── uploadRoutes.js # /api/uploads
│   │
│   ├── middleware/      # Request processors
│   │   ├── auth.js      # JWT verification
│   │   ├── error.js     # Error handling
│   │   ├── validation.js # Input validation
│   │   └── upload.js    # File upload config
│   │
│   ├── utils/           # Helper functions
│   │   ├── jwt.js       # Token utilities
│   │   └── cloudinaryUpload.js # Image utilities
│   │
│   ├── app.js           # Express app setup
│   └── server.js        # Server entry point
│
├── package.json         # Dependencies
├── .env                 # Environment variables
└── .gitignore          # Git ignore patterns
```

## 🎯 Technology Stack

```
┌─────────────────────────────────────┐
│         Backend Stack                │
└─────────────────────────────────────┘

Runtime Environment
    └─ Node.js v16+

Web Framework
    └─ Express.js 4.18.2

Database
    ├─ MongoDB (NoSQL)
    └─ Mongoose 8.0.3 (ODM)

Authentication
    ├─ JWT (JSON Web Tokens)
    └─ bcryptjs (Password hashing)

Image Storage
    └─ Cloudinary API

Security
    ├─ Helmet (Security headers)
    ├─ CORS (Cross-origin)
    ├─ Rate Limiting
    └─ Input Validation

Utilities
    ├─ Multer (File upload)
    ├─ Compression (Response compression)
    ├─ Morgan (HTTP logging)
    └─ dotenv (Environment config)
```

## 🚀 Performance Optimizations

```
Database Level:
  ├─ Indexes on frequently queried fields
  ├─ Pagination for large result sets
  ├─ Lean queries (plain JS objects)
  └─ Connection pooling

API Level:
  ├─ Response compression (gzip)
  ├─ Rate limiting (prevent abuse)
  ├─ Efficient query patterns
  └─ Minimal data transfer

Image Level:
  ├─ Cloudinary auto-optimization
  ├─ Lazy loading support
  ├─ Multiple format support
  └─ CDN delivery
```

This architecture provides a **scalable, secure, and maintainable** backend system! 🎉
